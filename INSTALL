== PREREQUISITES ==

Dependences:
  - libraw
  - pyside
  - python-dev
  - shiboken

To install the dependencies in Ubuntu 12.04:

  sudo apt-get install libraw-dev \
    python-pyside libpyside-dev \
    shiboken libshiboken-dev \
    python-dev


== BUILDING ==

First, configure the CFITSIO library:

  cd cfitsio
  ./configure

then ABC:

  cd ..
  qmake

and finally build the whole project:

  make

